/**
 * Esquemas — blueprint dual grid (mayor 60px + menor 12px via .slide::before).
 * The minor grid lives in the format CSS; the major grid uses .slide::before from
 * the base CAROUSEL_CSS via --grid-line token. This block adds the minor grid.
 */
export const ornaments = `
<div class="format-ornaments-blueprint" aria-hidden="true"></div>
`;
