/**
 * Minimalista — gradient orbs of accent colors that bleed through the glass.
 */
export const ornaments = `
<div class="format-ornaments-orbs" aria-hidden="true" style="
  position:absolute;inset:0;pointer-events:none;z-index:0;filter:saturate(1.2);
  background:
    radial-gradient(circle at 80% 20%, rgba(200,255,0,0.55), transparent 30%),
    radial-gradient(circle at 20% 80%, rgba(255,106,54,0.55), transparent 35%),
    radial-gradient(circle at 60% 60%, rgba(150,80,200,0.30), transparent 40%);
"></div>
`;
